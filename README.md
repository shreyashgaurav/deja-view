# DejaView

Finds the photos you've seen before: a fast duplicate & near-duplicate image
detector in C++20, combining perceptual hashing with a learned ML classifier.
Report-only: never modifies your files.

🚧 Under active development. Roadmap and design docs in `docs/`.
